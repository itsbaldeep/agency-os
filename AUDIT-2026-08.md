# Agency OS VPS Audit — 2026-08

Audit timestamp: 2026-08-22 14:20 UTC  
Host: `srv1771622` (`deployden.tech`)  
Authority: `CEO_DIRECTIVE.md` → canonical `ROADMAP.md` → live state  
Scope: onboarding and read-only audit of the single Agency OS VPS

## Audit contract and status

- Phase 1 was read-only. Phase 2 created only this report.
- No service, container, schedule, database row, client-facing record, firewall
  rule, Caddy route, credential, repository history, or deployment was changed.
- Secret values were not read into the report or printed. Environment inventory
  below is limited to variable names and presence/absence.
- Privileged evidence was gathered only through the root-owned, allow-listed
  `/usr/local/sbin/codex-system-audit` helper.
- Full-history gitleaks scans used redaction and the repository config.
- Phase 3 execution log: **active — approvals received 2026-08-22**.

Severity used here: CRITICAL, HIGH, MEDIUM, LOW, INFO. Proposed actions are
labelled:

- **SAFE** — reversible and no data loss.
- **CAREFUL** — reversible, but touches a live service or automation path.
- **DESTRUCTIVE** — deletes data/artifacts or has an irreversible edge; requires
  explicit line-item approval.

## Live inventory snapshot

### Host and systemd

- Ubuntu 26.04 LTS, kernel `7.0.0-29-generic`, uptime 16 days.
- Root filesystem: 193 GiB total, 51 GiB used (27%); inode use 3%.
- RAM: 15 GiB total, approximately 3.4 GiB used at snapshot; swap 507 MiB used.
- 31 system services were running, 58 service units were enabled, one unit was
  failed, and 21 enabled units were not active.
- Agency-specific running/enabled units: `agency-worker`, `agency-bot`,
  `caddy-ask`, `caddy`, `opencode`, and `headscale`.
- Failed: `systemd-networkd-wait-online.service`, failed since 2026-08-06.
- The 21 enabled/non-active units are standard template, socket-activated, or
  one-shot services except the failed wait-online unit: `cloud-init-main`,
  `dmesg`, `e2scrub_reap`, `getty@`, `grub-initrd-fallback`, `grub2-common`,
  `open-iscsi`, `open-vm-tools`, `pollinate`, `secureboot-db`, five Snap units,
  `sshd-keygen`, `systemd-networkd-wait-online`, `systemd-pstore`,
  `ua-reboot-cmds`, `ubuntu-advantage`, and `vgauth`.
- User systemd has only the user D-Bus running; no Agency workload is hidden in
  user services.

### Cron and background jobs

- The `agency` crontab is byte-for-byte equivalent in content to
  `infra/cron/agency.crontab`.
- No other user has a crontab.
- System cron adds normal run-parts entries and a root weekly
  `docker builder prune -f` at 01:02 Saturday.
- Jobs 1–11 and 13–14 have active cron lines. Job 12's line is commented.
- Database `background_jobs`: jobs 1–11 and 13–14 are enabled; only job 12 is
  disabled.

### Containers

- `docker ps -a` and `infra/docker/container-manifest.tsv` match exactly: 20
  named containers in each, all 20 running.
- Those 20 include six Aetheria containers and nine Hearth containers.
- Agency OS Compose declares `postgres`, `clickhouse`, and `minio`, but only
  `postgres` and `clickhouse` exist. `agency-minio` has never been brought into
  the running Compose project and is absent from the manifest.
- Docker storage: 12.48 GiB images (6.354 GiB reclaimable), 5.658 GiB writable
  container layers, 1.596 GiB volumes, and no build cache.

### Data plane

- Postgres database `agencyos`: 98 MiB logical database; about 163 MiB data
  directory. Largest tables are `health_checks` (48 MiB, 552,415 live rows) and
  `job_runs` (39 MiB, about 155,809 live rows).
- Tasks: 174 `done`, 88 `failed`, no queued/running/unknown rows, and no current
  stuck or orphaned row.
- ClickHouse client data: `ai_visibility_checks` 150 rows and `events` 137 rows.
  Visibility rows are brand 1=60, brand 2=60, brand 4=30.
- ClickHouse system telemetry: 1.99 GiB/171.7 million rows, dwarfing the 29.4
  KiB/287 rows of actual Agency data.

### Caddy and network

- Live `/etc/caddy/Caddyfile` imports ten fragments from
  `/home/agency/agency-os/caddy-apps/*.caddy`; configuration validates.
- All configured hostnames resolve and return an HTTP response through Caddy.
  The four Aetheria names and five Hearth names are therefore genuinely public,
  not dead config. Root-path 404/403 responses on API/admin/storage endpoints
  still prove that Caddy reached a live upstream.
- `*.apps.deployden.tech` is a live wildcard DNS record: a random name resolves
  to the VPS.
- UFW is active: default deny incoming/routed, allow outgoing. Public allowances
  are SSH 22, HTTP 80, HTTPS 443, Tailscale UDP 41641, and Headscale TCP 8080.
  Database, dashboard, Adminer, OpenCode, and parked-app preview ports bind only
  to `100.64.0.1`; Aetheria HTTP upstreams bind to loopback.

### Repositories

Checked-out Git repositories under `/home/agency/projects`:

| Repository | Branch | Worktree | Size | Ledger reality |
|---|---|---:|---:|---|
| aetheria | main | clean | 254 MiB | project 29, `scaffolded` |
| agency-dashboard | main | clean | 4.5 MiB | project 15, `idea` |
| agency-os | main | clean | 9.2 MiB | project 14, `idea` |
| deployden | main | clean | 388 KiB | project 10, `archived`, although public/live |
| hearth | master | clean | 385 MiB | project 4 `live` plus project 16 archived duplicate |
| streamwise | `fix/worker-22-...` | clean | 4.1 GiB | project 5 `preview` plus project 17 archived duplicate |

The deployed `/home/agency/agency-os` checkout is clean and on the same commit
as the source checkout (`4e38efb`). `/home/agency/projects/dashboard`, recorded
in the running Adminer container's Compose labels, no longer exists.

### Task reliability, last 30 days

| Task type | Total | Failed | Failure rate |
|---|---:|---:|---:|
| onboard_project | 2 | 2 | 100.0% |
| generate_draft | 8 | 6 | 75.0% |
| content_outline | 14 | 10 | 71.4% |
| content_compose | 6 | 3 | 50.0% |
| run_brand_audit | 5 | 2 | 40.0% |
| agent_task | 11 | 4 | 36.4% |
| competitor_scan | 6 | 2 | 33.3% |
| content_research | 6 | 2 | 33.3% |
| self_review | 3 | 1 | 33.3% |
| ask | 13 | 2 | 15.4% |
| defend_audit | 7 | 1 | 14.3% |
| propose_fix | 104 | 14 | 13.5% |

Seven failures were explicitly caused by worker restarts/orphaning (two
`onboard_project`, two `agent_task`, three `propose_fix`). All are now marked
failed; none is still running.

### Token use since the 2026-08-22 migration deployment

- `token_usage`: one `deepseek-chat` row, 817 input + 155 output tokens,
  $0.000237.
- Task rows over the same deployed period: 31,832 prompt + 1,750 completion
  tokens, $0.000764 across seven tasks.
- `OPENCODE_FALLBACK` is unset. `DEEPSEEK_API_KEY` and `OPENAI_BASE_URL` are
  declared. `ZAI_API_KEY`, `OPENROUTER_API_KEY`, and
  `OPENROUTER_FREE_MODEL` are not declared.

# 1. Drift — docs, database, cron, and reality disagree

## D-01 — Deploy jobs are enabled and continuously executing

**Severity: HIGH**

Evidence: CEO directive §7 and both roadmaps say jobs 8 and 9 are disabled.
Cron schedules them every two and three minutes, and both database rows are
enabled. Over seven days job 8 ran 5,010 times (328 failures) and job 9 ran
3,340 times. The current user prompt correctly warned that they auto-deploy.

Proposed action: **CAREFUL** — decide the canonical policy. At minimum, disable
8/9 in `background_jobs` before Phase 3 multi-commit batches, perform one batched
deploy, re-queue any restart-orphaned task, then restore the explicitly approved
policy. Update the directive and roadmap to match.

## D-02 — Parked meta-automation is live

**Severity: HIGH**

Evidence: roadmap Parked says auto-merge/self-building meta-loops are not client
value. Job 10 (`self-review`) and job 11 (`auto-merge`) are enabled and scheduled;
job 11 ran 3,360 times in seven days and auto-merged agency-os PR 75 on the audit
day. This is autonomous write authority, not dead code.

Proposed action: **CAREFUL** — disable jobs 10 and 11 in the DB and comment their
cron entries as one reversible scheduler batch. Preserve scripts in `_archive`
or Git history.

## D-03 — Background-job metadata is not authoritative

**Severity: MEDIUM**

Evidence: job 13 is `30 2 * * 1` in the database but runs at `30 4 * * 1` in
cron. Job 12 is hourly in the DB but its commented cron example is every 30
minutes. `requires_approval=true` on job 6 is not enforced by `run-job.sh`.

Proposed action: **CAREFUL** — make one source authoritative and generate/check
the other. Enforce `requires_approval` deterministically or remove the misleading
field. Keep job 12 disabled.

## D-04 — Project ledger states do not describe the live estate

**Severity: HIGH**

Evidence: Aetheria is `scaffolded`, Hearth is `live`, and Streamwise is
`preview`, although the directive calls them killed/archived. Deployden is
`archived` despite a live public site. Core Postgres, ClickHouse, and dashboard
services are attached to archived project 3 (`dashboard`) rather than project 15
(`agency-dashboard`). Hearth and Streamwise each have duplicate archived repo
rows. Every project `local_path` is empty.

Proposed action: **SAFE** — after service cleanup decisions, reconcile project
states, deduplicate aliases without deleting rows, populate correct local paths,
and record which row owns core services. This is ledger cleanup only; take a DB
backup immediately before the batch.

## D-05 — Services and health state are stale/incomplete

**Severity: HIGH**

Evidence: five Streamwise services remain `running` in `services` despite no
Streamwise containers since 2026-08-06. Only one Aetheria service exists and it
has no container name, so none of six Aetheria containers receives health rows.
Deployden and Adminer are absent from the service ledger. The health script only
checks current containers with a matching service row and never updates
`services.status` or `last_seen`.

Proposed action: **CAREFUL** — rebuild service inventory from the manifest/live
Docker labels, mark absent services stopped/archived, and make health checks
update current state plus state transitions. Do not manufacture historical
health.

## D-06 — Caddy disaster-recovery source is incomplete

**Severity: MEDIUM**

Evidence: live `/etc/caddy/Caddyfile` contains the global TLS configuration and
fragment import, but both deployed and source `infra/caddy/Caddyfile` files are
empty. Fragment copies do match each other.

Proposed action: **SAFE** — version a secret-free canonical base Caddyfile and
add a validation/drift check. Do not overwrite `/etc/caddy/Caddyfile` until a
separate **CAREFUL** reload step is approved.

## D-07 — GSC key path in docs is wrong and no connector consumes it

**Severity: HIGH**

Evidence: docs repeatedly say `gcs-api-key.json`. The actual file is
`/home/agency/agency-os/gsc-api-key.json`; no code references either filename.
It is mode 0664. No connector exists, consistent with the roadmap's functional
status but not with its pathname/security claim.

Proposed action: **SAFE** — immediately restrict the actual file to 0600 in the
approved security batch, choose one canonical filename, and correct docs. Treat
connector implementation as roadmap work, not cleanup.

## D-08 — Documented LLM fallback availability is aspirational

**Severity: HIGH**

Evidence: fallback code exists, but both fallback provider key names are absent
from the live environment. Today the real raw-completion chain is DeepSeek only;
when its balance/rate limit fails, both configured fallback entries are skipped.

Proposed action: **SAFE** — correct the state docs to say “code path present,
credentials absent.” Adding provider credentials is a separate human action.

## D-09 — Compose and manifest disagree about Agency MinIO

**Severity: MEDIUM**

Evidence: Compose declares `agency-minio` on Tailscale port 9010, but it is not
created, not in the 20-container manifest, and not in the service ledger.

Proposed action: **SAFE** — document it as a migration target and add it to the
manifest only after it is deliberately deployed. Do not silently start it.

## D-10 — Visibility counts in handoff docs are stale

**Severity: LOW**

Evidence: docs say brand 4 has 15 ClickHouse rows; live state has 30 after the
2026-08-22 audit. Brands 1 and 2 remain 60 each.

Proposed action: **SAFE** — update state blocks after Phase 3; do not hand-edit
client data.

# 2. Parasites and dead weight

## P-01 — Aetheria remains a complete live public stack

**Severity: HIGH**

Sole-purpose inventory:

- Repository: `/home/agency/projects/aetheria` (254 MiB).
- Runtime artifacts: `/home/agency/aetheria` (195 MiB; 192 MiB client builds).
- Containers: `aetheria-adminserver`, `aetheria-portal`,
  `aetheria-gameserver`, `aetheria-authserver`, `aetheria-postgres`,
  `aetheria-redis`.
- Persistent volume: Aetheria Postgres 67.4 MiB; Redis negligible.
- Caddy fragments: `admin.aetheria...`, `aetheria...`, `api.aetheria...`, and
  `play.aetheria...` (four files in both source and deployed checkouts).
- DNS-facing DB rows: four live `dns_records`; wildcard DNS also resolves.
- Ledger: project 29 (`scaffolded`), one incomplete service row, 12 approvals,
  including one `aetheria_screens` approval stuck in `approved`.
- Scheduler/code: background job 12 (disabled), commented cron line,
  `scripts/aetheria-loop.sh`, the disabled worker handler block,
  `infra/migrations/002_aetheria_approval_types.sql`, seed/schema references,
  dashboard `/aetheria` routes and two Aetheria templates, dashboard Compose
  bind mount of Aetheria docs, four Caddy files, and manifest entries.
- Systemd units: none dedicated to Aetheria.

Cost at snapshot: about 55 MiB RAM, roughly 450 MiB repo/runtime artifacts plus
volume/images, six containers, four public sites, four private listener ports,
and unmonitored operational surface.

Proposed action: **CAREFUL** — first archive a state/export note and database
dump, then stop (do not remove) the six containers, set restart policies to
`no` or bring the Compose stack down without volumes, disable the four Caddy
fragments, change four DNS ledger rows away from live, and archive the project
row. Keep job 12 disabled.  
Optional disk reclamation: **DESTRUCTIVE** — remove Aetheria images, volumes,
build artifacts, or repo checkout only with separate explicit approval after
the archive is verified.

## P-02 — Job/resume automation is disabled in worker dispatch but still live in UI/data

**Severity: HIGH**

Sole-purpose inventory:

- Postgres tables and live-row counts: `job_applications` 1,
  `job_campaigns` 1, `job_contacts` 6, `job_listings` 1,
  `job_run_history` 1, `cover_letters` 1, `resume_versions` 1,
  `linkedin_notes` 0, `email_threads` 1. Last activity was 2026-07-17.
- Worker bundle: all files in `scripts/jobs/` (`campaign.py`, `contacts.py`,
  `cover_letter.py`, `gmail_auth.py`, `gmail_client.py`, `linkedin_mcp.py`,
  `resume.py`, `schema.py`, `__init__.py`) plus seven retained handler blocks in
  `worker.py`. Compiled `__pycache__` files also remain.
- Dashboard: live `/jobs` routes in `app.py`, job query functions in
  `models.py`, and `jobs.html`, `job_new.html`, `job_detail.html`,
  `job_contacts.html`, `job_applications.html`, plus job fragments.
- System Map still advertises all killed task types.
- Personal artifact tree: `/home/agency/simranjt` — 8.3 MiB, 255 files (PDF,
  DOCX, TXT, TeX and supporting files). Content was not opened.
- Containers, Caddy, DNS, cron, systemd: none dedicated.

The hidden routes can still create campaigns/OAuth state and enqueue
`run_job_campaign`, `find_contacts`, `generate_resume`, and
`generate_cover_letter`; the worker will deterministically fail them because
their dispatch entries are removed.

Proposed action: **SAFE** — make all legacy `/jobs` mutation routes return a
clear 410/disabled response and remove killed types from System Map while
preserving read-only history.  
Archive: **CAREFUL** — move the worker/dashboard implementation bundles to an
`_archive` area in a coordinated code deploy.  
Data removal: **DESTRUCTIVE** — dropping tables or deleting personal artifacts
requires separate approval; prefer encrypted/off-host archive first.

## P-03 — Hearth is mostly dead weight, but its storage is now a core dependency

**Severity: CRITICAL (cleanup ordering)**

Sole-purpose inventory for the parts that are still purely Hearth:

- Repository `/home/agency/projects/hearth` (385 MiB, including 316 MiB of
  `.next` builds and 65 MiB of project-local OpenCode files).
- Eight separable containers: three web frontends, two APIs, Postgres, Redis,
  and ClickHouse.
- Five Caddy fragments/hostnames: API, business, dev, customer, and images.
- Ledger: project 4 `live`, duplicate project 16 archived, nine service rows,
  two executed approvals, no `dns_records` rows (wildcard DNS supplies reach).
- Systemd/cron: none dedicated.

Snapshot cost for all nine Hearth containers: about 870 MiB RAM. The Hearth
ClickHouse alone uses about 422 MiB/512 MiB and its volume is 1.418 GiB. Active
web writable layers total about 1.15 GiB; additional old Hearth images account
for a large share of Docker's 6.354 GiB reclaimable image space.

Exception: `hearth-storage` is **not solely Hearth anymore**. The Agency content
pipeline is configured to use it, its configured bucket exists, and one of
eight content items contains a direct Tailscale-only storage URL. Stopping it
now would break that asset. `agency-minio` is only declared, not running.

Proposed action: **CAREFUL** — migrate/copy the Agency bucket to deliberately
started `agency-minio`, change the content-pipeline endpoint/public URL, verify
existing asset retrieval without rewriting client content, then stop the eight
pure Hearth containers and later stop `hearth-storage`. Disable public Hearth
Caddy fragments only after storage URL dependencies are resolved.  
Disk reclamation: **DESTRUCTIVE** — remove old Hearth images/volumes/builds only
after exported DB/ClickHouse/MinIO backups and explicit approval.

## P-04 — Streamwise is stopped but consumes the most obvious reclaimable disk

**Severity: MEDIUM**

Inventory:

- Repository: 4.1 GiB on a WIP parking branch, including multiple nested
  `node_modules` trees (well over 3 GiB) and roughly 344 MiB of `.next` output.
- Four unused images: `streamwise`, `streamwise-admin`, `streamwise-blog`, and
  `streamwise-web`, approximately 2.6 GiB of unique image layers.
- Two unused volumes are negligible in size.
- No running container, Caddy fragment, DNS row, cron entry, or systemd unit.
- Ledger still says project 5 `preview`; all five service rows say `running`.
  Duplicate project 17 is archived.

Proposed action: **SAFE** — mark the project/services archived/stopped and record
the parking branch/commit.  
Reclaim builds and images: **DESTRUCTIVE** — remove regenerable `node_modules`,
`.next`, unused images, and orphan volumes only with explicit line-item approval.
Preserve Git history or an archive bundle.

## P-05 — Other archived project debris is small but confusing

**Severity: LOW**

Evidence: archived rows remain for `vercel-next-js`, `chalk-chalk`, two coffee
landings, subscription-sharing-platform, old dashboard, Hearth repo, Streamwise
repo, and Deployden. `/home/agency/archive` contains three old product tarballs,
a Chalk archive, and a plaintext Hearth environment snapshot. A non-Git
`/home/agency/projects/hello-world` tree (12 KiB) has no ledger row. Deployden
must not be treated as dead because it is the serving public site.

Proposed action: **SAFE** — produce a small archive index tying each artifact to
its ledger row and exempt live Deployden/core dashboard.  
Deletion: **DESTRUCTIVE** — remove archive bundles/orphan files only after
explicit approval and checksum/off-host copy.

## P-06 — High-frequency meta jobs manufacture attention

**Severity: MEDIUM**

Evidence: jobs 8, 9, and 11 alone created 11,710 `job_runs` rows in seven days;
jobs 1–3 add 20,160. `jobs.log` is 18.2 MiB and has no observed dedicated
rotation. `job_runs` is 39 MiB.

Proposed action: **CAREFUL** — disable parked meta jobs, log “no change” checks
less frequently or only on state transitions, add bounded retention, and add
logrotate. Preserve a summarized audit trail.

# 3. Risk

## R-01 — Agency ClickHouse is burning a CPU core on self-telemetry

**Severity: CRITICAL**

Evidence: two snapshots 15+ minutes apart showed approximately 100% CPU and
743–763 MiB RAM with about 695 PIDs. It has restarted three times. Actual Agency
data is 29.4 KiB, while system tables are 1.99 GiB/171.7 million rows:
`text_log` 1.78 GiB/51.7 million rows, `asynchronous_metric_log` 62 MiB/117.3
million rows, plus other logs. Container log files add 2.1 GiB; current plain
logs alone were about 1.4 GiB. `MEMORY_LIMIT_EXCEEDED` had occurred 14,529 times.
There is no TTL on the largest system tables. The dominant messages are the
system-log merge loop itself.

Proposed action: **CAREFUL** — first add conservative log levels/TTLs or disable
unneeded ClickHouse system logs, validate config, then restart ClickHouse during
a declared window and verify the 150 visibility/137 event rows.  
Existing telemetry cleanup: **DESTRUCTIVE** — truncate/drop old system-log
partitions and rotate container logs only with explicit approval; do not touch
`default.ai_visibility_checks` or `default.events`.

## R-02 — Backups cover only Postgres and remain on the same disk

**Severity: CRITICAL**

Evidence: nine nightly Postgres gzip dumps exist and all pass gzip integrity.
No ClickHouse, Headscale, content object storage, Caddy base config, or
off-host backup was found. No restore drill is recorded. `pg-dump.sh` lacks
`set -o pipefail`, so a failed `pg_dump` can be masked by successful gzip. The
bootstrap note recommends `pg_restore`, but the produced files are plain SQL
streams and require `psql` after decompression.

Proposed action: **CAREFUL** — fix pipeline error handling, document the correct
restore command, run a restore drill into an isolated temporary database, and
add encrypted off-host backups for Postgres, ClickHouse client tables,
Headscale state, and the Agency content bucket. Never bundle live secrets into
Git.

## R-03 — Dashboard compromise is host-root compromise

**Severity: HIGH**

Evidence: dashboard listens only on Tailscale, but returns 200 without app-level
authentication. Its container runs as root, has a writable root filesystem, and
mounts `/var/run/docker.sock`, the deployed scripts, Caddy fragments, and
Aetheria docs read-write. Any dashboard code-execution flaw can control Docker
and therefore the host.

Proposed action: **CAREFUL** — run the app as a non-root UID, make code/config
mounts read-only, replace raw Docker socket access with a minimal read-only
metrics proxy, and add an authentication/CSRF boundary for mutation routes.
Deploy and test as one dashboard batch.

## R-04 — Privileged-account surface is wider than Codex needs

**Severity: HIGH**

Evidence: the new Codex helper is root-owned, 0755, rejects invalid modes, and is
appropriately allow-listed. However, `ubuntu` retains `NOPASSWD:ALL`; `agency`
also has passwordless unrestricted iptables commands and several duplicate
systemctl rules. Root SSH key login is permitted (`prohibit-password` wins over
the later-looking legacy line); password login is disabled.

Proposed action: **CAREFUL** — confirm the operational need for `ubuntu`
passwordless root and unrestricted iptables, then narrow/remove redundant rules
with a tested second SSH session and rollback file. Keep the audit helper.

## R-05 — Several real credential files are world-readable

**Severity: CRITICAL**

Evidence without reading values:

- `gsc-api-key.json` is 0664.
- Streamwise root/app `.env` files and built standalone copies are 0644 and name
  variables including database URLs, auth secrets, and API keys.
- Deployden `.env` is 0644 and names a webhook variable.
- Hearth preview `.env` and its archive copy are 0644 and name DB/JWT/API/MinIO
  secrets.
- Core Agency `.env`, bot `.env`, Codex auth, and root OpenCode env are correctly
  0600.

Proposed action: **SAFE** — chmod live/archive credential-bearing files to 0600,
inventory duplicate build copies, and ensure parent directories are not shared.
Do not rotate/delete until usage is mapped. If any file was ever exposed beyond
the account boundary, credential rotation is **CAREFUL** and provider-specific.

## R-06 — The security scanner is incomplete and can mutate repos without its approval flag

**Severity: HIGH**

Evidence: gitleaks full-history scans found zero findings in all six repos using
the repository config. However, that config excludes `.env` files—the exact
place where world-readable secrets exist. The scheduled scanner only scans
three repos because of root-level package-file detection, uses `--no-git`, skips
tests, and runs global `pip-audit` rather than project environments. Most
seriously, it runs `npm audit fix` automatically despite job 6 being marked
`requires_approval=true`; `run-job.sh` ignores that flag.

Proposed action: **CAREFUL** — immediately make job 6 report-only, enforce its
gate, scan all six repos plus a names/permissions-only credential audit, and
separate dependency detection from mutation. Any dependency updates should use
normal reviewed repo changes.

## R-07 — Public Headscale uses cleartext HTTP on port 8080

**Severity: MEDIUM**

Evidence: UFW intentionally allows 8080 worldwide; Headscale listens on
`0.0.0.0:8080`, advertises `http://187.127.182.199:8080`, and has no TLS cert/key
configured. Metrics and gRPC remain loopback-only.

Proposed action: **CAREFUL** — place Headscale behind TLS/Caddy or configure
native TLS following a tested migration plan for existing clients. Do not close
8080 until client connectivity is verified.

## R-08 — Package/reboot maintenance is pending

**Severity: MEDIUM**

Evidence: 23 packages are upgradable, including AppArmor, Docker Compose,
Tailscale, GRUB, Snap, and guest tools. `/var/run/reboot-required` is present.
Package lists were refreshed on the audit day.

Proposed action: **CAREFUL** — schedule upgrades and a reboot with explicit
health checks for SSH, Tailscale/Headscale, Docker, Postgres, ClickHouse, Caddy,
dashboard, worker, and bot. Snapshot current state first.

## R-09 — The control plane is a single point of failure

**Severity: HIGH**

Evidence: one VPS hosts DNS ingress, VPN coordination, databases, worker, bot,
dashboard, public sites, and the only located backups. No external monitor,
replica, or off-host restore target was found.

Proposed action: **CAREFUL** — prioritize off-host backups and an external
synthetic check before adding a secondary Headscale/database node. Document
manual recovery order.

## R-10 — Telemetry and scheduler tables have no bounded retention

**Severity: MEDIUM**

Evidence: `health_checks` has 552k rows/48 MiB and currently adds 7,200 rows per
day, mostly for parked Hearth. `job_runs` has 155k rows/39 MiB. ClickHouse system
telemetry is already pathological. No retention cleanup was found.

Proposed action: **CAREFUL** — define per-table retention and preserve daily
aggregates/state changes before pruning. Initial deletion of historical rows is
**DESTRUCTIVE** and needs a specific date boundary and approval.

## R-11 — Service monitoring reports green on a partial inventory

**Severity: HIGH**

Evidence: current daily health rows are all healthy because the probe enumerates
only running containers and inserts only when a matching service exists. It
cannot report a disappeared container and ignores most Aetheria plus Deployden.
This is structurally incapable of detecting the failure mode it claims to
monitor.

Proposed action: **CAREFUL** — probe the declared expected inventory, record
missing containers as unhealthy, add HTTP/readiness checks, and alert on state
transitions. Keep black-box external checks separate from Docker process checks.

# 4. Broken windows

## B-01 — Suggestion Approve is a no-op status flip

**Severity: HIGH**

Evidence: `POST /api/suggestions/<id>/approve` only updates
`suggestions.status='approved'` and traces the event. It creates no task or
execution record. This confirms the known Phase 0 leftover.

Proposed action: **CAREFUL** — define deterministic suggestion-to-task mapping,
validate brand access/capability gates, create exactly one idempotent task, link
it to the suggestion, and show the task on the report. Do not apply retroactively
to client suggestions without a separate review.

## B-02 — Brand activity events are written under generic project names

**Severity: MEDIUM**

Evidence: audit creation traces `project='brands'`, while engagement activity
queries `project=<brand name>`. Other onboarding traces use `clients`. The
brand page therefore cannot retrieve its own event stream. This confirms the
second known Phase 0 leftover.

Proposed action: **CAREFUL** — add stable brand/project identifiers to event
schema or use a deterministic canonical project key. Backfill only if approved;
the code fix should not rewrite client-visible content.

## B-03 — Approved actions can remain invisible and retry forever

**Severity: HIGH**

Evidence: approvals 46 (`content`, since 2026-07-11) and 61
(`aetheria_screens`, since 2026-08-15) are `approved` but unexecuted. The
executor handles only `dns`, `deploy`, and `apex-deploy`; unknown types are
skipped every minute and never transition. The dashboard's action list shows
only `pending`, hiding approved limbo. Ten old unsupported approvals could
starve supported ones because the executor selects the oldest ten.

Proposed action: **CAREFUL** — give unsupported types an explicit
`manual_required`/`failed` terminal state, surface approved-unexecuted items,
and prevent starvation. Do not change the two rows until the human decides
whether to reject, archive, or implement them.

## B-04 — Hidden job UI creates tasks that have no handler

**Severity: HIGH**

Evidence: `/jobs` is absent from navigation but returns live pages; its POST
routes insert killed task types. Corresponding DISPATCH entries are commented,
so every such task fails with “No handler for type.” OAuth and campaign mutation
routes also remain active.

Proposed action: **SAFE** — return 410/disabled for all mutation endpoints and
retain read-only history until the archive batch is approved.

## B-05 — Service status says stopped Streamwise is running

**Severity: HIGH**

Evidence: five Streamwise rows have status `running`; Docker has zero Streamwise
containers. See D-05/R-11.

Proposed action: **SAFE** — reconcile the ledger from live evidence and render
“archived/stopped” rather than green.

## B-06 — Token dashboard is materially incomplete after migration

**Severity: HIGH**

Evidence: post-migration task rows recorded 33,582 tokens; `token_usage` recorded
972 (2.9%). The successful Codex `ask` task alone recorded 29,362 task tokens but
no `codex` ledger row. `run_brand_audit` and one `defend_audit` also lack model
ledger rows. Only `propose_fix`, Aetheria, and `defend_audit` have explicit insert
sites.

Proposed action: **CAREFUL** — centralize token recording in the worker's task
finalization path with model/project/brand attribution and idempotency. Do not
fabricate historical per-brand attribution; an auditable backfill can use task
totals only.

## B-07 — “Fallback chain” has no live fallback credentials

**Severity: HIGH**

Evidence: see D-08. The code logs and skips both absent providers, leaving
DeepSeek as the only raw completion host.

Proposed action: **SAFE** — dashboard should show provider readiness based on
key presence without exposing values, so “configured” and “available” are not
conflated.

## B-08 — Agency content storage emits private-only URLs

**Severity: HIGH**

Evidence: current storage target is `hearth-storage`; configured public base is
the direct Tailscale storage endpoint, and one content item references it. Such
an asset cannot work on a public client site. The public
`images.hearth.apps.deployden.tech` Caddy site exists, but no content item uses
that hostname.

Proposed action: **CAREFUL** — migrate to an Agency-owned storage hostname with
explicit bucket policy and TLS, then make new asset URLs public/reachable by
design. Do not rewrite existing content records without line-item approval.

## B-09 — Job approval flags do not gate execution

**Severity: HIGH**

Evidence: `run-job.sh` selects only `enabled=true`; it does not read
`requires_approval`. Job 6 runs and can mutate repos despite the flag.

Proposed action: **CAREFUL** — implement a deterministic scheduled-approval
contract or remove mutation from scheduled jobs. Never silently reinterpret old
approval rows.

## B-10 — High content-pipeline validation failure rates are visible but not managed

**Severity: MEDIUM**

Evidence: last-30-day failure rates are 75% for `generate_draft`, 71.4% for
`content_outline`, and 50% for `content_compose`, mostly deterministic schema or
keyword validation failures. The validators are doing their job; retry/repair
policy is not.

Proposed action: **CAREFUL** — keep validators, add bounded structured repair
retries and dashboard failure grouping, and test against fixtures before live
client runs.

## B-11 — Restart-orphan handling is reactive, not automatic recovery

**Severity: MEDIUM**

Evidence: seven last-30-day tasks failed explicitly as “Worker restarted — task
orphaned.” No current rows are stuck, but deploy restarts remain frequent and
tasks are not automatically re-queued.

Proposed action: **CAREFUL** — before every worker restart record running task
IDs; after restart re-queue only idempotent types or require human review for
side-effecting types. Preserve failure history.

## B-12 — Health/job logs record volume, not useful state change

**Severity: MEDIUM**

Evidence: 7,200 identical healthy rows/day and tens of thousands of “up to date”
job runs/week produce green noise while missing absent services.

Proposed action: **CAREFUL** — store transitions and periodic rollups, not every
unchanged poll; keep raw logs only for a short bounded window.

# 5. OpenCode retirement readiness

## O-01 — OpenCode web is still enabled and running

**Severity: HIGH**

Evidence: `opencode.service` is active/enabled on Tailscale `:4096`, protected by
Basic auth, with no active TCP client at audit time. The process used about 313
MiB RAM. The current dashboard has no nav link, but the legacy user manual and
root context still advertise it.

Proposed action: **CAREFUL** — after the human confirms the web UI is no longer
used, stop and disable (do not uninstall) `opencode.service`. Verify worker
`ask`, `agent_task`, Discord `!ask`/`!run`, and Codex subscription auth before
and after. Rollback: enable/start the unit.

## O-02 — The intentional fallback path is correctly isolated

**Severity: INFO**

Evidence: `OPENCODE_FALLBACK` is unset; `run_agent_harness` defaults to Codex.
The Codex child environment strips raw-completion API variables and uses the
subscription auth file. Codex and Node symlinks exist in `/usr/local/bin`; a
post-fix `ask` task succeeded with 29,362 input tokens. The earlier missing-Node
failure is resolved.

Proposed action: **SAFE** — retain `run_opencode`, the binary, auth/config, and a
tested rollback note until Codex has survived an agreed soak period.

## O-03 — Dependencies beyond `OPENCODE_FALLBACK` remain

**Severity: HIGH**

Active or reconstructive references outside the escape hatch:

- The disabled Aetheria handler invokes the OpenCode binary directly and uses
  OpenCode model names.
- `design_page` calls a script under
  `~/.config/opencode/skills/ui-ux-pro-max/...`; that task remains in dispatch.
- `infra/bootstrap.sh` installs/enables OpenCode; `sync-system-state.sh`
  snapshots its unit/config.
- `caddy-ask.service` uses an environment file named `opencode.env` even though
  it is a Postgres dependency.
- Discord command docstrings/help still say OpenCode; dashboard System Map says
  `propose_fix` uses OpenCode and advertises Aetheria's headless OpenCode loop.
- `agency-os-user-manual.md`, `/home/agency/projects/AGENTS.md`, and
  `SETUP-CONTEXT.md` are OpenCode-era. `/home/agency/AGENTS.md` does not yet
  exist.

Proposed action: **CAREFUL** — decouple the design helper into an Agency/Codex
path, archive the Aetheria direct handler with the project, make bootstrap Codex
first, rename the generic Caddy env file, and update live bot/dashboard labels
in one batched deploy. Keep only the explicit fallback references.

## O-04 — OpenCode state consumes disk but cannot yet be deleted

**Severity: MEDIUM**

Evidence: approximately 755 MiB across `.local/share/opencode` (439 MiB),
`.opencode` (237 MiB), `.config/opencode` (64 MiB), and `.cache/opencode`
(15 MiB), plus project-local Hearth OpenCode files. This includes the retained
fallback and design-helper dependency.

Proposed action: **SAFE** — inventory which directories are required by fallback
and the design helper; rotate/compress logs if supported.  
Removal: **DESTRUCTIVE** — do not delete OpenCode binary/auth/session/config data
until O-03 is resolved and the human explicitly retires the fallback.

## O-05 — Retirement verdict

**Severity: MEDIUM**

The VPS is ready to **disable the OpenCode web service carefully**, because the
worker and Discord agent harness are demonstrably on Codex and no current web
connection exists. It is **not ready to uninstall OpenCode or delete its data**:
the rollback path, active design helper, disabled Aetheria code, bootstrap, and
operational docs still depend on its files/names.

Proposed action: **CAREFUL** — execute O-01 first, soak, then O-03. Leave
`OPENCODE_FALLBACK` available throughout; never re-enable job 12.

## Suggested Phase 3 order (not approval and not executed)

1. **SAFE security/documentation batch:** credential file modes, truthful state
   docs, archive index, and dashboard/provider readiness text that requires no
   worker restart.
2. **CAREFUL automation freeze:** disable jobs 8/9 for the work window and
   disable parked jobs 10/11; make scanner report-only before other repo work.
3. **CAREFUL ClickHouse stabilization:** stop telemetry growth before cleanup.
4. **CAREFUL broken-window code batch:** approvals, job-route lockout, service
   inventory, token accounting, suggestion approval, activity keying. One
   agency-os push/restart, with running-task capture and orphan re-queue.
5. **CAREFUL storage disentanglement:** migrate Agency content away from Hearth.
6. **CAREFUL parked-stack stop:** Aetheria and separable Hearth services; remove
   public Caddy routes only after verification.
7. **CAREFUL OpenCode web disable and soak.**
8. **DESTRUCTIVE disk reclamation only by explicit sub-item:** old images,
   volumes, generated builds, telemetry partitions, or archived data.

## Rollback template for the Phase 3 execution log

For every approved batch, append:

```text
Timestamp:
Approved finding IDs:
Pre-state / backup:
Commands or commit:
Verification:
Rollback command/path:
Running task IDs before restart:
Re-queued task IDs after restart:
Result:
```

## Phase 3 execution log

### Batch 1 — retire autonomous core-repository loops

```text
Timestamp: 2026-08-22 17:40 UTC
Approved finding IDs: D-01, D-02, P-06, B-11
Pre-state / backup: jobs 8-11 enabled; no queued/running/stuck/orphaned tasks;
  /home/agency/backups/manual/agency-os-pre-automation-freeze-20260822.dump
Commands or commit: background_jobs 8-11 set enabled=false; cron entries
  commented in source and deployed copies; user crontab installed from source.
Verification: database rows 8-12 are disabled; installed user crontab has jobs
  8-12 commented; no active task needed re-queueing.
Rollback: restore the database dump or set the four rows enabled=true, restore
  the four cron lines from Git, then reinstall the crontab. Do not roll back
  job 12; it remains disabled.
Running task IDs before restart: none
Re-queued task IDs after restart: not applicable; no restart performed
Result: autonomous deploy/self-review/auto-merge scheduling retired.
```

### Batch 2 — bound ClickHouse telemetry

```text
Timestamp: 2026-08-22 17:45 UTC
Approved finding IDs: R-01, R-10
Pre-state / backup: 1 CPU / 2 GiB limit; self-telemetry consumed gigabytes;
  /home/agency/backups/clickhouse/agency-ch-user-backup-20260822.tar.gz
Commands or commit: disabled all ClickHouse system log tables; reduced concurrency,
  memory, and background pools; recreated only agency-clickhouse; removed regenerated
  system-log tables after restart.
Verification: container limit 0.25 CPU / 512 MiB; ClickHouse data directory 1.1 MiB;
  system.parts has zero active system-log parts; default.events retains 137 rows and
  default.ai_visibility_checks retains 150 rows.
Rollback: restore the four config/compose files from Git, recreate agency-clickhouse,
  and restore the Native-format user-table bundle if user data is ever affected.
Running task IDs before restart: none
Re-queued task IDs after restart: not applicable; worker was not restarted
Result: useful Agency datasets retained; runaway self-observation removed.
```

### Batch 3 — central credentials and recovery control

```text
Timestamp: 2026-08-22 17:52 UTC
Approved finding IDs: R-02, R-05, D-07, B-06, B-07
Pre-state / backup: project-local .env files; GSC JSON mode 0664; Postgres-only
  gzip backup with no pipefail; no off-site acknowledgement or rotation state.
Commands or commit: moved Agency credentials to /home/agency/.config/agency
  (directory 0700, files 0400) with compatibility symlinks; removed an embedded
  engagement token from dashboard Compose; added one stdlib operations CLI for
  core backups, checksum verification, Saturday off-site acknowledgement, and
  credential-name/rotation auditing; replaced the daily digest with exception-led
  recovery, failure-rate, content-reliability, and accounting signals.
Verification: five operations unit tests pass; a 8.9 MB core bundle was created
  and checksum-verified with Postgres, both ClickHouse Agency datasets, credentials,
  and readable configuration. Root-only state is honestly marked absent pending
  the fixed-target backup-core helper.
Rollback: move the central files back to their original symlink paths; restore the
  scripts from Git; the previous Postgres dump remains in Git history and prior
  backups remain under /home/agency/agency-os/backups.
Running task IDs before restart: none
Re-queued task IDs after restart: not applicable; no worker restart
Result: central credential source and verifiable recovery bundle active; root-only
  Headscale/OpenCode state remains the single declared recovery gap.
```

### Batch 4 — tracked approval, publication, failure, and accounting workflows

```text
Timestamp: 2026-08-22 18:16 UTC
Approved finding IDs: B-01, B-03, B-06, B-09, B-10, B-11
Pre-state / backup: full core backup from Batch 3; schema migration applied in a
  transaction; no queued/running tasks immediately before the worker restart.
Commands or commit: core commit `fix: link approvals content and task outcomes`;
  dashboard commit dd2a91d; rebuilt only agency-dashboard; installed the worker
  and approval executor into the deployed checkout; restarted only agency-worker.
Verification: 10 core and 3 dashboard workflow tests pass; all 44 templates parse;
  migration link columns/indexes exist; approval-executor SQL passed a rollback-only
  database self-test; dashboard health, task detail, and operations return HTTP 200;
  worker PID changed 381842 -> 737051 and is active; source/deployed scripts match;
  no queued, running, orphaned, or needs-input task remained after restart.
Rollback: revert the two named commits; restore the prior deployed worker/executor
  from Git; rebuild agency-dashboard; restart agency-worker once after confirming
  no live tasks. Migration columns are additive and can safely remain; dropping
  them would be destructive and is not required for code rollback.
Running task IDs before restart: none
Re-queued task IDs after restart: none
Result: Approve now creates a linked execution/publication task; missing inputs are
  visible and resumable in place; failures carry deterministic first aid and alert
  Discord; restart recovery separates safe tasks from external-side-effect tasks;
  task-level token accounting has one guarded write path.
```

### Batch 5 — isolate core storage and park deprecated engagements

```text
Timestamp: 2026-08-22 18:41 UTC
Approved finding IDs: P-01 through P-05, B-04, B-05, B-08, O-02
Pre-state / backup: verified Git/data bundles under
  /home/agency/backups/engagements/{hearth,streamwise,aetheria}; a separate
  jobs-SaaS PostgreSQL dump under /home/agency/backups/retired/weft-predecessor;
  checksums and each Git bundle verified before any removal.
Commands or commit: started core agency-minio; copied and checksum-compared the
  three Agency content objects; made assets.apps.deployden.tech authoritative and
  kept images.hearth.apps.deployden.tech as a compatibility alias; added explicit
  project classification/lifecycle/recovery metadata; stopped all nine Hearth
  containers without removing containers or volumes; archived Hearth Caddy routes;
  removed Aetheria routes, six exact containers, two exact volumes, four exact
  images, and its two source/runtime directories after recovery verification;
  dropped only the nine isolated jobs-SaaS tables; removed the dead /jobs,
  /aetheria, Adminer, and ClickHouse-play dashboard surfaces; retired job 12 to
  /usr/bin/false; installed the tested reduced worker and restarted it once.
Verification: core and legacy asset URLs return HTTP 200 with matching objects;
  Hearth containers are Exited (0) and its volumes remain; Aetheria containers,
  volumes, source, and routes are absent; estate rows say Hearth/Streamwise
  soft_parked and Aetheria hard_parked; OpenCode remains active on
  100.64.0.1:4096; agency-worker is active on the new code; 11 core tests pass;
  no queued, running, or orphaned tasks were created (two approvals remain
  intentionally needs_input).
Rollback: restore Aetheria from its Git/runtime/PostgreSQL/Redis bundle and restore
  archived Caddy files only if the engagement is explicitly revived; start the
  retained Hearth containers from its recorded Compose manifest to unpark it;
  revert the core/dashboard commits and rebuild/restart only after checking tasks;
  restore the jobs-SaaS dump only into a future isolated Weft database, never the
  core Agency schema.
Running task IDs before restart: none
Re-queued task IDs after restart: none
Result: core content no longer depends on Hearth; parked engagements use explicit,
  recoverable lifecycles; obsolete job/Aetheria surfaces no longer masquerade as
  Agency capabilities; OpenCode web and fallback remain retained.
```

### Batch 6 — replace idle telemetry with actionable operations

```text
Timestamp: 2026-08-22 18:51 UTC
Approved finding IDs: R-01, B-01, B-02, B-10
Pre-state / backup: 156,000+ mostly repetitive job-run rows; verified custom dump
  /home/agency/backups/operations/job-ledger-pre-compaction-2026-08-22;
  full retired-engagement snapshot under
  /home/agency/backups/retired/legacy-engagements/2026-08-22.
Commands or commit: retired the once-per-minute approval poller; changed every
  approval type to enqueue a linked worker task; made deploy approvals atomically
  write, validate, and reload Caddy with rollback; made DNS approvals request a
  real provider and credential reference instead of falsely marking records live;
  reduced health checks to 15 minutes with state-change/hourly retention; kept the
  memory guard at five minutes but discard no-op runs; compacted retained job history
  to daily state plus failures; fixed engagement activity keys from project=brands
  to brand:<id>; replaced the dashboard's repetitive event feed with service/task/job
  incidents, input requests, and rolling content reliability; removed all other
  hard-parked estate rows after a full DB snapshot.
Verification: 12 core and 3 dashboard tests pass; scheduled no-op memory probe leaves
  job_runs count unchanged; the live database has 329 meaningful job-run rows after
  compaction; dashboard /dashboard/data and engagement activity return HTTP 200;
  only Deployden, Technoflavour, Hearth, and Streamwise appear as engagements;
  agency-worker restarted on the tested code with zero queued/running/orphaned tasks.
Rollback: restore the job-ledger dump for operational history; restore the full
  retired-engagement dump if an archived marketing dataset is intentionally revived;
  revert core/dashboard commits, reinstall cron/scripts, rebuild dashboard, and
  restart the worker only after capturing live tasks.
Running task IDs before restart: none
Re-queued task IDs after restart: none
Result: successful no-ops no longer dominate the dashboard or database; approvals
  are always tracked, live operations are validated, and current failures surface
  as attention rather than being buried in raw events.
```

### Batch 7 — truthful model routing, evidence, and observable spend

```text
Timestamp: 2026-08-22 19:30–19:59 UTC
Approved finding IDs: D-08, B-06, B-07, B-10
Pre-state / backup: full verified core bundles from Batches 3/5; no running task
  before each worker-only deployment.
Commands or commits: core 3d62234, 02c5584, b008b83, c3b6ce4, a5421bb;
  dashboard 516da84 and 4723bda; migrations 010–012. Replaced retired DeepSeek
  aliases with V4 Flash/Pro, enabled deterministic JSON mode, corrected OpenCode
  CLI flags and provider-error detection, isolated raw API credentials from
  OpenCode OAuth, retained billable usage from rejected completions, added task/job
  usage attribution, actual model-chain labels, and database-enforced competitor
  uniqueness/stale-proposal reconciliation.
Verification: direct DeepSeek Flash JSON probe succeeded (41 in / 6 out in the
  initial probe); OpenCode OpenAI OAuth returned the exact probe while OpenCode
  Zen returned 401; 20 core tests passed at this batch boundary; Spend linked
  task 287 to deepseek-v4-flash,deepseek-v4-pro with 2,194 in / 3,794 out and
  $0.001826. Dashboard /spend/data and /tasks/287 returned HTTP 200.
Rollback: revert the named commits and rebuild dashboard; restart the worker only
  with an empty queue. Additive attribution columns/indexes can remain. Restoring
  duplicate competitors is neither required nor useful; prior audit summaries
  retain the removed proposals.
Running task IDs before restart: none
Re-queued task IDs after restart: none
Result: every new task-level model run is attributable to its task and actual
  route; fallback auth no longer leaks provider credentials; repeated audits do
  not create duplicate/stale direct-competitor rows.
```

### Batch 8 — evidence-gated content and reduced control-plane attack surface

```text
Timestamp: 2026-08-22 19:15–19:57 UTC
Approved finding IDs: R-01, R-03, B-03, B-08, B-10, B-12
Pre-state / backup: verified core/engagement recovery bundles and empty worker
  queue before restarts.
Commands or commits: core b73c1f0 and content/model changes in 3d62234;
  dashboard 99d1e99, 2770993, 90c5c59. Added exact fetched-evidence snippets,
  fact-linked data blocks, typed outline/compose validators, a 24,000-token ceiling,
  explicit image sourcing, and no-write preview GET. Published a secret-free host
  health snapshot, removed the dashboard Docker socket, switched dashboard to a
  non-root user, and filtered routine successful ClickHouse heartbeats. Dropped the
  empty ClickHouse rankings table and empty Postgres assistant_messages,
  brand_embeddings, and mentions tables.
Verification: dashboard runs as user dashboard with zero Docker-socket mounts;
  /, /resources, /system-map, /spend/data returned HTTP 200; ClickHouse retains
  only events and ai_visibility_checks; security scan across core, Deployden,
  Hearth, and Streamwise found zero high-signal/gitleaks findings. OpenCode web
  remained active at 100.64.0.1:4096.
Rollback: restore the named source commits and rebuild only agency-dashboard;
  reintroducing the Docker socket or empty/noise tables is not part of rollback.
  Database drops are recoverable from the pre-cleanup core dump.
Running task IDs before restart: none
Re-queued task IDs after restart: none
Result: content claims now require source evidence, GET previews are side-effect
  free, and a dashboard compromise no longer implies direct Docker control.
```

### Batch 9 — Jobright black-box acceptance pilot and first-aid fixes

```text
Timestamp: 2026-08-22 19:45–20:18 UTC
Approved finding IDs: B-06, B-10 plus the final Jobright acceptance request
Pre-state / backup: core-backup-20260822T194228Z.tar.gz, SHA-256
  ac8c47865958ce4a1c47cedee86ec9bac897b6430db8731e437a2c6cbd611990;
  Postgres, ClickHouse user tables, core MinIO, credentials, OpenCode state, and
  readable configuration all verified. root_state=false is the declared helper gap.
Commands or commits: core 43a4bb7, dbf6952, 6432847, 33c746f, 9f97a56,
  c80903b, 73c7a8d. Ran Jobright tasks 285–297. Task 285 intentionally failed
  0/15 and exposed unbounded visibility output plus fallback auth contamination;
  task 286 proved the bounded fix; task 287 proved direct-substitute selection and
  completed audit 32. Research 288 fetched five pages and retained four exact facts.
  Outline failures 289/290 exposed contradictory/count-ignoring model output;
  task 291 passed after deterministic capping from 22 to 18 blocks. Compose failures
  292–296 exposed hidden-metadata keyword checks, absent persistence, and alternate
  JSON shapes; accepted blocks were checkpointed and task 297 resumed at block 13.
Verification: audit 32 completed 15/15 with zero query failures: Jobright 1,
  Teal 9, LazyApply 5, Simplify 4 citations (explicit DeepSeek training-knowledge
  proxy only). Draft 20 is status=draft, never published, 18 blocks, 1,129 words,
  three source-linked evidence blocks, four public sources, and zero validator
  failures. All four evidence snippets were independently refetched and matched.
  /content/20/preview, /content/new?outline=20, /tasks/297, /spend/data, and
  /engagements/brand/28/report returned HTTP 200. Task 297 used 8,756 input and
  937 output tokens on deepseek-v4-pro for $0.002471; no publish_content task exists.
Rollback: the pilot brand/audit/research/draft are non-published ledger fixtures;
  retain them as acceptance evidence. Revert the named code commits and restart
  only with an empty queue if checkpoint/parser behavior must be rolled back.
Running task IDs before restart: none before each restart; failed tasks were terminal
Re-queued task IDs after restart: none; retries/resumes were explicit new linked tasks
Result: the black-box audit and one-outline/one-draft path are proven end-to-end;
  the pilot converted every encountered failure into deterministic validation,
  attribution, or resume behavior instead of hiding it. Multiple variant selection,
  image review, and external publication remain honestly unproven.
```

## Jobright.ai external black-box baseline — offense and defense

Scope: public web only on 2026-08-22. No Jobright GA4, GSC, code, database,
account, backlink provider, real SERP feed, or conversion data was available.
Jobright's user/interview/time/job-volume figures below are first-party marketing
claims, not independently verified outcomes.

### Defensible public strengths

- The homepage presents a broad end-to-end product: matches, autofill, tailored
  resumes, insider connections, and career copilot. It claims 2M users, 8M jobs,
  400k new daily jobs, 3x interviews, and 80% time saved:
  https://jobright.ai/.
- It has a deliberate bottom-funnel comparison program. Eleven comparison URLs
  appeared in `sitemap-comparison.xml`; `/compare/teal` is a long, current page
  with a structured feature table, ratings, FAQs, and product calls-to-action.
- It owns a large long-tail job taxonomy. The three taxonomy sitemap files exposed
  4,548 + 2,274 + 2,274 URL entries in this snapshot. That is substantial organic
  distribution surface, although this audit did not test duplication, indexation,
  traffic, or page quality.
- Product/editorial pages are crawlable. `robots.txt` allows `/tools/*`, `/blog/*`,
  job/role/remote-job paths for general crawlers while protecting account/profile,
  matching, API, and monitoring surfaces: https://jobright.ai/robots.txt.
- Direct competitors are not weak. Simplify publicly offers matching, tailored
  resumes, autofill, networking, tracking, and a large job corpus
  (https://simplify.jobs/); Huntr offers tailored resumes/cover letters, autofill,
  job/contact/interview tracking, and match scoring (https://huntr.co/). Weft cannot
  win merely by adding a resume generator and job tracker.

### Public technical/content windows

- The homepage has one H1, canonical, Open Graph metadata, and Organization JSON-LD.
  Its meta description contains the visible typo `applilcations`; no hreflang was
  detected in the fetched homepage snapshot.
- The root sitemap index listed pages, comparisons, blog, three taxonomy files,
  and remote jobs. The dedicated blog sitemap exposed only 16 URLs even though
  public category/search discovery showed many more current articles. This is a
  testable sitemap-coverage gap, not proof that the missing posts are unindexed.
- Taxonomy scale is also a risk: thousands of programmatic URLs demand evidence of
  unique intent, canonical discipline, useful content, and crawl-budget control.
  Those checks require a multi-page crawl and real GSC index/traffic data.
- `robots.txt` blocks `GPTBot` and `ClaudeBot` only from `/jobs/`, blocks Bytespider
  entirely, and leaves product/editorial areas generally crawlable. That is a
  defensible privacy/cost choice, but it narrows direct model access to the job
  corpus; impact is unknown without real engine citation tests.
- The Jobright-authored Teal comparison calls itself “unbiased” while scoring
  Jobright as the clear winner. This is effective commercial content but weak
  independent proof; a transparent methodology is an opening for a challenger.
- Agency OS's directional DeepSeek proxy cited Jobright only 1/15 times versus
  Teal 9/15, LazyApply 5/15, and Simplify 4/15. It is a baseline for repeatability,
  not market share and not evidence about ChatGPT/Google visibility.

### Weft offense when its PRD arrives

1. Do not clone Jobright's breadth. Start with one measurable wedge: auditable,
   JD-specific resume + cover-letter quality with explicit user control, then
   traceable Gmail/LinkedIn follow-up workflows. OAuth, consent, privacy, and rate
   limits must be product requirements, not growth hacks.
2. Compete on proof. Show why each edit was made, which JD evidence it answers,
   what was sent, what remains human-approved, and downstream response/interview
   outcomes. Never repeat unverified “ATS optimized” or interview-lift claims.
3. Attack bottom-funnel intent before generic job-taxonomy scale: transparent
   Jobright/Teal/Simplify/Huntr comparisons, workflow-specific alternatives,
   privacy/control pages, JD-tailoring methodology, and real case studies.
4. Fill the evidence-backed gaps from research 5: buyer selection framework,
   data/privacy comparison, independently defined effectiveness metrics, and
   integration/workflow coverage. Draft 20 is a baseline artifact, not publishable
   market research until a human reviews every claim and angle.
5. Build AEO as citeable product documentation plus structured answers and real
   outcome datasets. Test each engine separately; never collapse a DeepSeek proxy,
   live-web results, GSC, and conversion analytics into one “visibility” number.

Challenge to the original ambition: beating Jobright is not currently an SEO task.
It requires a better product wedge, permission-safe integrations, reliable outcome
instrumentation, and time. Agency OS can make the research/execution loop cheaper
and more repeatable; it cannot manufacture product-market fit or first-party data.

### Batch 10 — final integrity, maintenance visibility, archive, and publication

```text
Timestamp: 2026-08-22 20:24–20:42 UTC
Approved finding IDs: R-02, R-05, B-06, B-10, plus approved workspace cleanup
Pre-state / backup: core-backup-20260822T202318Z.tar.gz, SHA-256
  d596c9e6383db1664a3234a720d8f71d29cca004d895844dd7d5a654c76d8e91;
  all application components verified, root_state=false.
Commands or commits: core cc71458, d2f8c5f, 8f3a491, bc4a8e6;
  dashboard 373de03 and 92f30c4. Applied migration 013. Rebuilt only the
  dashboard, restarted the worker once with an empty queue for cc71458, and did
  not restart it for host-snapshot/dashboard-only changes. Moved three stale
  runtime manuals/prompts to archive/runtime-metadata/legacy-docs-20260822 and
  nine superseded PostgreSQL-only dumps to
  backups/manual/legacy-postgres-dumps-20260822; nothing was deleted.
Data integrity: removed 27 duplicate/stale current-state brand_properties rows,
  retained the newest value for each brand/property identity, added a database
  uniqueness constraint, and changed worker/dashboard writes to upsert. Audit
  history remains in the audit ledger. Live result: 13 rows / 13 unique identities.
Observability: the secret-free host snapshot now carries package/reboot state and
  is mode 0644 behind a 0755 state directory so the isolated non-root dashboard
  can read its read-only mount. Resources shows 23 pending package updates and
  Required reboot; final Discord digest run 156648 returned HTTP 204 with 10
  action alerts. Credentials remain mode 0400 and are not part of this snapshot.
Verification: 24 core tests pass; Python/Jinja compilation passes; dashboard is
  non-root with no Docker socket; worker/bot/OpenCode/Caddy are active; /,
  /resources/data, /spend/data, /content/20/preview, /tasks/297, and the Jobright
  report return HTTP 200. There are zero queued/running/orphaned tasks and zero
  duplicate brand-property identities. ClickHouse contains only events (153 rows
  at final inventory) and ai_visibility_checks (180 rows), using about 35 KiB.
Publication: agency-dashboard PR #16 passed CI and merged; Streamwise PR #1 merged
  the soft-park context and removed 2.8M tracked vendored-dependency lines. Hearth
  PR #4 remains deliberately unmerged: lint/typecheck pass, but unchanged product
  tests fail on missing tests/setup.ts and existing UUID/route expectations. The
  recovery branch is pushed; no failed check was bypassed and Hearth stays stopped.
  Agency OS PR #76 passed compile-check and merged. Final security-scan job run
  156653 completed across all five retained repositories with zero gitleaks/high-
  signal findings; Streamwise's parked root Dockerfile remains cosmetic debt.
Final recovery: core-backup-20260822T204129Z.tar.gz, 84,519,726 bytes, SHA-256
  bf9d6e2006e4bf9bb036a2e18fc0c48f3a0c48881f466f49ad2d41f04723f9a4.
  Independent verification returned ok=true for Postgres, events,
  ai_visibility_checks, core MinIO, credentials, OpenCode state, infra/config,
  AGENTS, directive, roadmap, capability registry, and latest audit;
  root_state=false remains explicit. Job run 156652 completed in 14 seconds.
Rollback: restore redundant property rows only from the pre-migration backup if a
  forensic need outweighs current-state integrity; otherwise revert the named
  commits, rebuild the dashboard, and restart the worker only with an empty queue.
  Move archived docs/dumps back to their recorded original paths if needed. Revert
  merged PRs normally; do not resurrect vendored node_modules or duplicate agent
  instructions in normal operation.
Running task IDs before restart: none
Re-queued task IDs after restart: none
Result: repeat audits are idempotent, maintenance debt is visible, recovery carries
  the latest audit context, archived clutter is recoverable, and core publication
  is manual/reviewed rather than coupled to automatic deployment.
```

### Residual operator-owned risks after Batch 10

| Severity | Evidence | Proposed action |
|---|---|---|
| HIGH | The latest all-round application backup still reports `root_state=false`; the fixed-target helper has no `backup-core` command. | **CAREFUL:** add that single fixed-target helper action, then verify a bundle containing units, firewall, sudo policy, Headscale state, and Caddy state. |
| HIGH | The first laptop/off-site copy is overdue for Saturday 2026-08-22. Discord continues to alert. | **SAFE:** SCP the latest verified bundle to the laptop, verify its SHA-256 locally, then manually acknowledge it in Operations. |
| HIGH | Central credential inventory still contains unrotated and placeholder-like entries; OpenCode Zen returns 401. No values were printed. | **CAREFUL:** human-rotate/acknowledge each named credential and replace Zen only if that free fallback is retained. |
| HIGH | `apt` reports 23 upgradable packages and `/var/run/reboot-required` names the installed kernel transition. | **CAREFUL:** schedule an operator-authorized upgrade/reboot, then verify firewall, sockets, containers, systemd, routes, queue, and a fresh backup. |
| MEDIUM | Headscale remains deliberately public on TCP 8080 cleartext and the `ubuntu` account retains broad passwordless sudo. | **CAREFUL:** decide the Headscale TLS/reverse-proxy migration and reduce the unrelated account's sudo scope after confirming its owner/workflows. |
| MEDIUM | Hearth retains 2.727 GiB of stopped container layers, a root-owned `.next` tree, and a pushed PR with pre-existing product-test failures. | **DESTRUCTIVE for layer/tree removal:** retain while rollback value is desired; fix only if Hearth is explicitly unparked. |
| LOW | `systemd-networkd-wait-online.service` has a stale boot failure while live networking and all required services are healthy. | **CAREFUL:** correct or disable the wait-online dependency during the scheduled reboot window, then verify boot ordering. |
| PRODUCT | Jobright visibility remains a DeepSeek proxy; content variant selection, reviewed images, real CMS publish/rollback, and Deployden first-party GSC/GA4 measurement are unproven. | **SAFE/CAREFUL by adapter:** follow ROADMAP order; do not claim these capabilities before live evidence exists. |
